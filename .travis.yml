language: "php"

php:
    - "7.1"
    - "7.2"

before_install:
    - "composer self-update"

install:
    - "composer install"

script:
    - "php vendor/bin/phpunit --coverage-clover=coverage.clover --coverage-text --verbose"
